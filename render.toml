services = [
  { type = "web", name = "agentic-ai-csv", env = "python", plan = "starter", buildCommand = "pip install -r requirements.txt", startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT", healthCheckPath = "/health", autoDeploy = true }
]